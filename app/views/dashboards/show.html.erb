<br />

<div class="booyah-box col-10 offset-1">
  <h1>What I'm Learning</h1>
    <br />
    <br />
    <% current_user.enrolled_courses.each do|course| %>
      <h3 class="text-center">
        <%= link_to course.title, course_path(course) %></h3>
        <br/>
        <%= image_tag course.image.url, class: 'img-fluid' %>
        <!-- put in image_tag before course.image to test and class: 'img-fluid' after -->
        <hr class="hr_item2"/>
        <div class="row">
          <div class="col-12 col-sm-8">
              <%= course.description %>
          </div>
          <div class="col-12 col-sm-4">
              <%= link_to "More Info...", course_path(course), class: 'btn btn-info' %>
          </div>
        </div>
        <hr class="hr_item2">
        <br class="clr" />
      <% end %>
    </div>
    <br />
    <div class="booyah-box col-10 offset-1">
      <h1>What I'm Teaching</h1>
      <br />
      <br />
      <% current_user.courses.each do |course| %>
        <h3 class="text-center">
          <%= link_to course.title, course_path(course) %>
        </h3>
        <br />
        <%= image_tag course.image.url, class: 'img-fluid' %>
        <br />
        <br />s
        <div class="row">
          <div class="col-12 col-sm-8">
              <%= course.description %>
          </div>
          <br />
          <div class="col-12 col-sm-4">
              <%= link_to "More Info...", course_path(course), class: 'btn btn-info' %>
          </div>
        </div>
        <hr class="hr_item2">
        <br class="clr" />
      <% end %>
    </div>
    <br />
    <br />